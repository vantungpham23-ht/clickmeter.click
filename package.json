{
  "name": "clickmeter-click",
  "version": "1.0.0",
  "scripts": {
    "build": "cd dashboard-client && npm install && npm run build && cp public/_redirects dist/dashboard-client/_redirects",
    "start": "cd dashboard-client && npm start",
    "dev": "cd dashboard-client && npm start"
  },
  "devDependencies": {
    "supabase": "^2.53.6"
  }
}
